from ImageStacker import ImageStacker
import ImageConvert

# How to convert a dir of JPG's to ppms and them stack the ppms
# ImageConvert.convertDir('test', 'JPG', 'out', 'imgtest')
# ImageStacker('imgtest', 2, 'out').stack()

# convert png's in test/ to ppms, and save as out/imgtest_XXX.ppm
# ImageConvert.convertDir('test', 'png', 'out', 'imgtest')

# convert ppms with name imgtest('imgtest_XXX.ppm') and stack, output in ./
# ImageStacker('imgtest', 12, 'out').stack()
